#include <stdio.h>
#include <stdlib.h>

#define N 10;

int main() {

    int i =0;
    float m = 0.0;
    double k = 0.0;
    long double d = 0.0;

    printf("\nsizeof int = %d\n", (int) sizeof(i));

    printf("\nsizeof float = %d\n", (int) sizeof(m));

    printf("\nsizeof double = %d\n", (int) sizeof(k));

    printf("\nsizeof long double = %d\n", (int) sizeof(d));

    printf("\n\n\n\n");

return 0;

}